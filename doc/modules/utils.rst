.. _utils:

===================
The utils submodule
===================

The utils submodule houses validation functions and other dataframe utilities.
The rule of thumb for the ``utils`` submodule is that for a function to be
added, it should require reasonably wide use across the entire package.

.. raw:: html

   <br/>
